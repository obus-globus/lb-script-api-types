import type { Object } from '../../../java/lang/Object.d.ts'
export class BlockUtil$IntBounds extends Object {
    constructor(min: number, max: number)
    max: number;
    min: number;
    toString(): string;
}