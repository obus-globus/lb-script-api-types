import type { Counter } from '../../../../../../com/oracle/truffle/regex/tregex/parser/Counter.d.ts'
export class Counter$ThreadSafeCounter extends Counter {
    constructor()
    dec(): number;
    dec(i: number): number;
    inc(): number;
    inc(i: number): number;
}