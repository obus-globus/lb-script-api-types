import type { Counter } from '../../../../../../com/oracle/truffle/regex/tregex/parser/Counter.d.ts'
export class Counter$ThresholdCounter extends Counter {
    constructor(max: number, errorMsg: string)
    // private errorMsg: string;
    // private max: number;
    inc(i: number): number;
}