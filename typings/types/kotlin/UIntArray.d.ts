import type { Object } from '../java/lang/Object.d.ts'
import type { UInt } from '../kotlin/UInt.d.ts'
import type { Iterator } from '../java/util/Iterator.d.ts'
export class UIntArray {
    // A14 statics-only surface: instances of this collection-backed type
    // render structurally (arrays / maps); only the statics are declared here.
// (invalid TS: name contains '-')     static box-impl(paramarg0: number[]): (Object | null)[];
// (invalid TS: name contains '-')     static constructor-impl(paramarg0: number): number[];
// (invalid TS: name contains '-')     static constructor-impl(paramarg0: number[]): number[];
// (invalid TS: name contains '-')     static contains-WZ4Q5Ns(element: UInt): boolean;
// (invalid TS: name contains '-')     static containsAll-impl(elements: UInt[]): boolean;
// (invalid TS: name contains '-')     static equals-impl(paramarg0: number[], paramarg1: Object): boolean;
// (invalid TS: name contains '-')     static equals-impl0(paramarg0: number[], paramarg1: number[]): boolean;
// (invalid TS: name contains '-')     static get-pVg5ArA(index: number): UInt;
// (invalid TS: name contains '-')     static getSize-impl(paramarg0: number[]): number;
// (invalid TS: name contains '-')     static hashCode-impl(paramarg0: number[]): number;
// (invalid TS: name contains '-')     static isEmpty-impl(): boolean;
// (invalid TS: name contains '-')     static iterator-impl(): Iterator<UInt>;
// (invalid TS: name contains '-')     static set-VXSXFK8(index: number, value: UInt): void;
// (invalid TS: name contains '-')     static toString-impl(paramarg0: number[]): string;
}