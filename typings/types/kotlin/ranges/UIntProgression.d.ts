import type { UIntProgression$Companion } from '../../kotlin/ranges/UIntProgression$Companion.d.ts'
export class UIntProgression {
    // A14 statics-only surface: instances of this collection-backed type
    // render structurally (arrays / maps); only the statics are declared here.
    static Companion: UIntProgression$Companion;
}