import type { UIntRange$Companion } from '../../kotlin/ranges/UIntRange$Companion.d.ts'
export class UIntRange {
    // A14 statics-only surface: instances of this collection-backed type
    // render structurally (arrays / maps); only the statics are declared here.
    static Companion: UIntRange$Companion;
}