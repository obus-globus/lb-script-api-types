import type { Object } from '../../../../../../../java/lang/Object.d.ts'
export class ULocale$AliasReplacer extends Object {
    constructor(language: string, script: string, region: string, variants: string, extensions: string)
    // private extensions: string;
    // private language: string;
    // private region: string;
    // private script: string;
    // private variants: string[];
    replace(): string;
    // private replaceLanguage(checkLanguage: boolean, checkRegion: boolean, checkVariants: boolean): boolean;
    // private replaceRegion(): boolean;
    // private replaceScript(): boolean;
    // private replaceSubdivision(subdivision: string): string;
    // private replaceTransformedExtensions(extensions: string): string;
    // private replaceVariant(): boolean;
}