import type { Object } from '../../../../../../../../java/lang/Object.d.ts'
export class XCldrStub$Joiner extends Object {
    static on(paramseparator: string): XCldrStub$Joiner;
    private constructor(separator: string)
    // private separator: string;
    join<T extends unknown>(source: T[]): string;
}