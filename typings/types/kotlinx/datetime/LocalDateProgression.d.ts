import type { LocalDateProgression$Companion } from '../../kotlinx/datetime/LocalDateProgression$Companion.d.ts'
export class LocalDateProgression {
    // A14 statics-only surface: instances of this collection-backed type
    // render structurally (arrays / maps); only the statics are declared here.
    static Companion: LocalDateProgression$Companion;
}