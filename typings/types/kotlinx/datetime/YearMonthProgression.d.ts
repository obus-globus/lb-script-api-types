import type { YearMonthProgression$Companion } from '../../kotlinx/datetime/YearMonthProgression$Companion.d.ts'
export class YearMonthProgression {
    // A14 statics-only surface: instances of this collection-backed type
    // render structurally (arrays / maps); only the statics are declared here.
    static Companion: YearMonthProgression$Companion;
}