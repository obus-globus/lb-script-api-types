import type { EventLoopGroupProxy$Companion } from '../../../../io/ktor/server/netty/EventLoopGroupProxy$Companion.d.ts'
export class EventLoopGroupProxy {
    // A14 statics-only surface: instances of this collection-backed type
    // render structurally (arrays / maps); only the statics are declared here.
    static Companion: EventLoopGroupProxy$Companion;
}